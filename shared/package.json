{
  "name": "@tasiki/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript contracts for the Tasiki server and mobile app.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
